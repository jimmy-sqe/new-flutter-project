name: shared
description: Shared utilities, mixins, base classes.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  async: ^2.11.0
  dartx: ^1.1.0
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  injectable: ^2.3.2
  rxdart: ^0.28.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.4.1
