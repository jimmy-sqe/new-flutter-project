class DurationConstants {
  const DurationConstants._();

  static const defaultEventTransformDuration = Duration(milliseconds: 300);
  static const defaultDebounceTimeDuration = Duration(milliseconds: 500);
  static const defaultThrottleTimeDuration = Duration(milliseconds: 500);
  static const errorVisibleDuration = Duration(seconds: 3);
}
