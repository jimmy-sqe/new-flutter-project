name: domain
description: Business logic - entities, use cases, repository interfaces.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  dartx: ^1.1.0
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  get_it: ^7.7.0
  injectable: ^2.3.2
  meta: ^1.15.0
  resources:
    path: ../resources
  rxdart: ^0.28.0
  shared:
    path: ../shared

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  freezed: ^2.5.0
  injectable_generator: ^2.4.1
  mocktail: ^0.3.0
